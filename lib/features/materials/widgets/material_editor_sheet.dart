import 'package:flutter/material.dart';

class MaterialEditorSheet extends StatelessWidget {
  const MaterialEditorSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
